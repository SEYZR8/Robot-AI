const c=document.getElementById('game'),r=new THREE.WebGLRenderer({canvas:c,antialias:true});r.setPixelRatio(Math.min(devicePixelRatio,1.7));r.setSize(innerWidth,innerHeight);r.shadowMap.enabled=true;const s=new THREE.Scene();s.background=new THREE.Color(0x07090d);s.fog=new THREE.Fog(0x07090d,35,220);const cam=new THREE.PerspectiveCamera(62,innerWidth/innerHeight,.1,500);const light=new THREE.HemisphereLight(0x9bbcff,0x241b16,1.8);s.add(light);const sun=new THREE.DirectionalLight(0xffe1b0,2.2);sun.position.set(-60,90,40);sun.castShadow=true;s.add(sun);const ground=new THREE.Mesh(new THREE.PlaneGeometry(500,500),new THREE.MeshStandardMaterial({color:0x26272a,roughness:.92}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;s.add(ground);function box(x,y,z,w,h,d,col){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),new THREE.MeshStandardMaterial({color:col,roughness:.75}));m.position.set(x,y,z);m.castShadow=m.receiveShadow=true;s.add(m);return m}for(let i=-9;i<=9;i++)for(let j=-7;j<=7;j++){if(Math.abs(i)%3===0||Math.abs(j)%3===0)continue;const h=4+Math.random()*13;box(i*12,h/2,j*12,8,h,8,0x53565b)}for(let i=-100;i<=100;i+=20){box(0,.03,i,10,.08,500,0x34363a);box(i,.04,0,500,.08,10,0x34363a)}const plaza=box(0,.12,-45,48,.2,30,0x827b6b);for(let i=0;i<6;i++){const col=box(-20+i*8,5,-45,4,10,4,0xb69d70);box(-20+i*8,11,-45,5,2,5,0x1c6171)}const car=new THREE.Group();const body=box(0,1,0,2.4,0.7,4.4,0x7b1d1d);car.add(body);for(const x of[-1,1])for(const z of[-1.5,1.5]){const w=new THREE.Mesh(new THREE.CylinderGeometry(.38,.38,.25,20),new THREE.MeshStandardMaterial({color:0x111111}));w.rotation.z=Math.PI/2;w.position.set(x,0.55,z);car.add(w)}car.position.set(0,.1,35);s.add(car);let speed=0,heading=Math.PI,keys={};addEventListener('keydown',e=>keys[e.key.toLowerCase()]=true);addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);document.querySelectorAll('[data-k]').forEach(b=>{const k=b.dataset.k;b.onpointerdown=()=>keys[k]=true;b.onpointerup=b.onpointercancel=()=>keys[k]=false});const fade=document.getElementById('fade'),story=document.getElementById('story'),title=document.getElementById('title'),mission=document.getElementById('mission'),controls=document.getElementById('controls'),skip=document.getElementById('skip');let intro=true;setTimeout(()=>fade.style.opacity=0,500);setTimeout(()=>{story.innerHTML='Samarqand tunlari jim. Ammo Jasurning telefoni yana bir bor jiringlaydi.<br><br><b>“Agar tirik qolishni istasang, eski ustaxonaga bor.”</b>';},3800);setTimeout(()=>{story.style.opacity=0;title.style.opacity=0;mission.hidden=false;controls.hidden=false;intro=false},7200);skip.onclick=()=>{story.style.opacity=0;title.style.opacity=0;mission.hidden=false;controls.hidden=false;intro=false;fade.style.opacity=0};function loop(){requestAnimationFrame(loop);if(!intro){const accel=keys.w?.55:keys.s?-.8:0;speed+=(accel-speed*.045);speed=Math.max(-3,Math.min(12,speed));if(keys.a)heading+=.035*(.3+Math.abs(speed)/8);if(keys.d)heading-=.035*(.3+Math.abs(speed)/8);car.rotation.y=heading;car.position.x-=Math.sin(heading)*speed*.035;car.position.z-=Math.cos(heading)*speed*.035}const target=new THREE.Vector3(car.position.x+Math.sin(heading)*-8,4.5,car.position.z+Math.cos(heading)*8);cam.position.lerp(target,.08);cam.lookAt(car.position.x,1,car.position.z);r.render(s,cam)}loop();addEventListener('resize',()=>{cam.aspect=innerWidth/innerHeight;cam.updateProjectionMatrix();r.setSize(innerWidth,innerHeight)});